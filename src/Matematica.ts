

export const Somar = (x: number, y: number) => {
    return x + y;
}
export const Subtrair = (x: number, y: number) => {
    return x - y;
}
export const Multiplicar = (x: number, y: number) => {
    return x * y;
}
